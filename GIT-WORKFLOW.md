# คู่มือ Git แบบ Manual — สำหรับโปรเจกต์ newtask-app

คู่มือนี้เขียนไว้สำหรับกรณีที่แก้โค้ดผ่าน Vibe Coding ใน VS Code แล้วต้องการรันคำสั่ง Git/Deploy เองทีละขั้น โดยไม่ต้องพึ่ง Claude Code ทุกครั้ง

ทุกคำสั่งด้านล่างให้รันจาก terminal ที่ root ของโปรเจกต์ (`C:\Users\wanna\newtask-app`)

---

## ภาพรวมลำดับขั้นตอน

```
แก้โค้ดใน VS Code
      ↓
1) ทดสอบในเครื่อง (local)
      ↓
2) Merge เข้า master
      ↓
3) Deploy + ทดสอบ Staging → ผ่านแล้วค่อย Deploy PRD
      ↓
4) Push ขึ้น GitHub + Bitbucket (สำรองโค้ด)
```

ข้อสำคัญ: **ห้ามข้ามลำดับ** โดยเฉพาะข้อ 3 — ต้องทดสอบ Staging ผ่านก่อนเสมอ ค่อย deploy PRD เพราะ PRD คือระบบจริงที่ทีมงานใช้งานอยู่ ถ้าพังจะกระทบคนใช้งานจริงทันที

---

## 1) แก้โค้ดเสร็จ → ทดสอบก่อน merge

### เช็กสถานะก่อนเสมอ

```bash
git status
```

**ทำไมต้องรัน:** จะได้เห็นว่าตอนนี้อยู่บรานช์ไหน (บรรทัดบนสุด `On branch ...`) และไฟล์ไหนถูกแก้ไปบ้าง (สีแดง = แก้แล้วแต่ยังไม่ได้ commit) ป้องกันความสับสนว่าตอนนี้ "อยู่ตรงไหน" ของงาน

### รันตรวจสอบพื้นฐาน 3 ตัว

```bash
pnpm typecheck
```

**ทำไมต้องรัน:** เช็กว่าโค้ด TypeScript มี type ผิดหรือไม่ (เช่น ส่งค่าผิดชนิด, เรียกฟังก์ชันผิด parameter) — จับบั๊กได้ก่อนที่จะรันจริงด้วยซ้ำ เร็วและปลอดภัยที่สุดในการเช็กรอบแรก

```bash
pnpm lint
```

**ทำไมต้องรัน:** เช็กมาตรฐานโค้ด (code style, การใช้ตัวแปรที่ไม่ได้ใช้, import ที่ไม่จำเป็น ฯลฯ) ช่วยจับความผิดพลาดเล็กๆ ที่ type-check มองไม่เห็น

```bash
pnpm test
```

**ทำไมต้องรัน:** รัน automated test ที่มีอยู่ในโปรเจกต์ (unit test / integration test) เพื่อยืนยันว่าฟีเจอร์เดิมที่เคยทำงานได้ ยังทำงานได้เหมือนเดิม ไม่ได้ถูกโค้ดใหม่ไปทำพัง (ป้องกัน regression)

> ถ้าคำสั่งไหนใน 3 ตัวนี้ "แดง" (error) แปลว่ายังไม่พร้อม merge — ต้องแก้ให้ผ่านก่อน

### รันแอปขึ้นมาทดสอบจริงด้วยตา

```bash
pnpm dev
```

**ทำไมต้องรัน:** 3 คำสั่งข้างบนเช็กแค่ "โค้ดถูกไวยากรณ์/ไม่พังของเดิม" แต่ไม่ได้การันตีว่าฟีเจอร์ใหม่ "ทำงานตามที่ตั้งใจ" จริงๆ ต้องเปิดเบราว์เซอร์ไปที่ localhost แล้วลองคลิกใช้งานฟีเจอร์ที่เพิ่งแก้ด้วยตัวเองก่อนเสมอ

---

## 2) โอเคแล้ว → Merge เข้า master

### Commit งานที่แก้ในบรานช์ปัจจุบัน (ถ้ายังไม่ commit)

```bash
git add -A
```

**ทำไมต้องรัน:** เป็นการ "เตรียม" ไฟล์ที่แก้ทั้งหมดให้พร้อมบันทึกเป็นประวัติ (`-A` = เอาทุกไฟล์ที่เปลี่ยนแปลง ทั้งแก้/เพิ่ม/ลบ)

```bash
git commit -m "อธิบายสั้นๆ ว่าแก้อะไร"
```

**ทำไมต้องรัน:** เป็นการ "บันทึก" การเปลี่ยนแปลงเป็นจุด checkpoint หนึ่งจุดในประวัติของโปรเจกต์ พร้อมข้อความอธิบาย (ควรเขียนให้สื่อว่าแก้อะไร เผื่อย้อนดูทีหลัง เช่น `fix: แก้บั๊กปุ่มบันทึกไม่ทำงานในหน้า Task`)

### สลับไป master แล้วดึงล่าสุดจาก GitHub มาก่อน

```bash
git checkout master
```

**ทำไมต้องรัน:** ย้ายไปยังบรานช์หลัก (`master`) ซึ่งเป็นบรานช์ที่ใช้อ้างอิงสำหรับ deploy ขึ้น Staging/PRD

```bash
git pull origin master
```

**ทำไมต้องรัน:** ดึงความเปลี่ยนแปลงล่าสุดจาก GitHub มาก่อน เผื่อมีคนอื่น (หรือเครื่องอื่น) push โค้ดเข้า master ไปแล้วโดยที่เครื่องนี้ยังไม่รู้ ถ้าข้ามขั้นตอนนี้แล้ว push ทีหลังอาจชนกัน (conflict) หรือทับงานคนอื่นโดยไม่ตั้งใจ

### Merge บรานช์ที่เพิ่งแก้เข้า master

```bash
git merge feature/xxx
```

**ทำไมต้องรัน:** แทน `feature/xxx` ด้วยชื่อบรานช์จริงที่เพิ่งแก้ไป (เช็กชื่อได้จาก `git status` ในขั้นตอนที่ 1) คำสั่งนี้จะเอาโค้ดที่ commit ไว้ในบรานช์นั้น มารวมเข้ากับ `master`

> **ถ้าขึ้น conflict** (Git บอกว่ามีบรรทัดที่ทั้งสองบรานช์แก้ทับกัน) — **อย่าเดาเอง** ให้หยุดแล้วแจ้งให้ช่วยดูให้ เพราะแก้ conflict ผิดจุดมีความเสี่ยงที่จะทำให้งานหายหรือทับของถูกต้องทิ้ง

---

## 3) ทดสอบ Staging ผ่าน → Deploy PRD

### Build โค้ด แล้ว deploy ขึ้น Staging

```bash
pnpm build
```

**ทำไมต้องรัน:** แปลงโค้ดต้นฉบับ (source code) ให้เป็นไฟล์ที่พร้อมใช้งานจริง (production build) — ไฟล์ที่ได้จากขั้นตอนนี้คือสิ่งที่จะถูกอัปโหลดขึ้นเซิร์ฟเวอร์จริง

```bash
npx wrangler deploy --env staging
```

**ทำไมต้องรัน:** อัปโหลดโค้ดที่ build แล้วขึ้น environment "staging" (โดเมน `staging.pronista.com`) ซึ่งเป็นระบบคู่ขนานสำหรับทดสอบก่อนของจริง ไม่กระทบผู้ใช้งานจริง (`--env staging` คือตัวบอก wrangler ว่าให้ deploy ไป environment ไหน)

**จากนั้น:** เข้าไปที่ `staging.pronista.com` แล้วทดสอบฟีเจอร์ที่แก้ด้วยตัวเองให้ครบ ทั้ง happy path (ใช้งานปกติ) และกรณีขอบๆ (เช่น กรอกข้อมูลว่าง, กดซ้ำ) — **ต้องผ่านหมดก่อน** ถึงจะไปขั้นต่อไปได้

### Deploy ขึ้น Production (PRD)

```bash
pnpm deploy
```

**ทำไมต้องรัน:** คำสั่งนี้เป็น shortcut ที่รวม `pnpm build` + `wrangler deploy` ไว้ในคำสั่งเดียว และเพราะไม่ได้ใส่ `--env` จึง deploy เข้า environment ค่าเริ่มต้นซึ่งคือ **production** (โดเมน `office.pronista.com`) — เป็นระบบจริงที่ทีมงานใช้งานอยู่ทุกวัน

> ทำขั้นนี้ **ต่อเมื่อ Staging ผ่านการทดสอบแล้วเท่านั้น**

---

## 4) Push ขึ้น GitHub + Bitbucket (สำรองโค้ด)

```bash
git push origin master
```

**ทำไมต้องรัน:** อัปโหลดประวัติ commit ของ `master` ขึ้น GitHub (remote ชื่อ `origin` ชี้ไปที่ `brandnista/Tasknista`) เพื่อสำรองโค้ดไว้และเป็นที่เก็บกลางให้ทีมดูประวัติได้

```bash
git push bitbucket master
```

**ทำไมต้องรัน:** อัปโหลดชุดเดียวกันขึ้น Bitbucket ด้วย (remote ชื่อ `bitbucket` ชี้ไปที่ `devnista/pronista`) — เก็บสำรองไว้อีกที่หนึ่งตามนโยบายของทีม

> **ข้อควรเข้าใจ:** ขั้นตอนนี้เป็นแค่การ "สำรองซอร์สโค้ด" (backup) — **ไม่ใช่การ deploy** ต่อให้ไม่ push เลย ระบบที่ deploy ไปแล้วในขั้นตอนที่ 3 ก็ยังทำงานได้ปกติ กลับกัน ต่อให้ push ขึ้น GitHub/Bitbucket แล้ว แต่ไม่ได้รัน `wrangler deploy`/`pnpm deploy` ระบบจริงก็จะยังเป็นโค้ดเวอร์ชันเก่าอยู่ ทั้งสองอย่างเป็นคนละเรื่องกัน ต้องทำทั้งคู่

---

## สรุปคำสั่งทั้งหมดเรียงตามลำดับ (copy ไปรันได้เลย)

```bash
git status
pnpm typecheck
pnpm lint
pnpm test
pnpm dev
```

```bash
git add -A
git commit -m "อธิบายว่าแก้อะไร"
git checkout master
git pull origin master
git merge feature/xxx
```

```bash
pnpm build
npx wrangler deploy --env staging
```

```bash
pnpm deploy
```

```bash
git push origin master
git push bitbucket master
```

---

## ข้อควรระวังโดยรวม

- **อย่าข้ามขั้นตอนที่ 1** (typecheck/lint/test) แม้จะรีบ — จับบั๊กได้เร็วและถูกกว่าไปเจอตอน Staging/PRD มาก
- **Staging ต้องผ่านก่อนเสมอ** ค่อย deploy PRD — ห้ามสลับลำดับ
- **เจอ conflict ตอน merge อย่าเดาเอง** — หยุดแล้วขอความช่วยเหลือก่อน
- **push ขึ้น GitHub/Bitbucket ≠ deploy** — เป็นคนละขั้นตอนกัน ต้องทำทั้งคู่ให้ครบ
